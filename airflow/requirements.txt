# ============================================================
# Kenya Health Facility Mapping Pipeline
# airflow/requirements.txt
#
# Installed into the Airflow worker image at build time.
# Pin versions to avoid surprise breakage on rebuild.
# ============================================================

# ── dbt Core + Trino adapter ─────────────────────────────────
# dbt runs inside the Airflow worker via BashOperator
dbt-core==1.8.0
dbt-trino==1.8.0

# ── Trino Python client ───────────────────────────────────────
# Used by dbt-trino and any direct Trino queries in DAGs
trino==0.328.0

# ── MinIO / S3 client ─────────────────────────────────────────
# boto3 talks to MinIO's S3-compatible API
boto3==1.34.0

# ── Ingestion scripts ─────────────────────────────────────────
requests==2.31.0
pandas==2.2.2

# ── Airflow providers ─────────────────────────────────────────
# Amazon provider gives S3Hook and S3 operators if needed later
apache-airflow-providers-amazon==8.19.0